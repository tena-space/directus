name: Build & Push to container registry

on:
  release:
    types: [created]
  push:
    branches:
      - "main"
    paths:
      - "Dockerfile"

jobs:
  docker:
    uses: kagchi/workflows/.github/workflows/docker-build.yml@main
    secrets: inherit